{
  "extends": "../../tsconfig.base.node.json",
  "include": ["src", "../../utils"],
  "exclude": ["node_modules"],
  "compilerOptions": {
    "incremental": true,
    "paths": {
      "@/*": ["./*"],
      "@shared/*": ["../../utils/*"]
    },
    // Must be the same as rollup output dir
    "outDir": "../api",
    "types": ["node", "express"]
  }
}
